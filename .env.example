# All keys are OPTIONAL — the app works without any of them via web scraping.

# OpenAI API key for image analysis + AI listing generation
# Get at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-key-here

# eBay API credentials (higher reliability than scraping)
# Register at https://developer.ebay.com
EBAY_APP_ID=your-ebay-app-id
EBAY_CERT_ID=your-ebay-cert-id

# Optional: eBay OAuth redirect URI for seller API access (auto-listing)
EBAY_REDIRECT_URI=

# Scanner settings
SCANNER_INTERVAL_MINUTES=10
SCANNER_ENABLED=true
